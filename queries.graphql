query Performers($id: String!) {
    festapp(id: $id) {
        name
        startsAt
    events(per: 5000, sortBy:"startsAt") {
            performers {
                name
            }
            name
            startsAt
        }
    }
}
